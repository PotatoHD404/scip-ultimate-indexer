# FAQ

## Authentication

Use `AuthenticationService` when you need login and session checks.

## Authentication

This repeated heading is intentional so the manual test can verify duplicate markdown headers do not collide.

## Database

Use `DatabasePool` for connection pooling and query execution.

## Installation

Install dependencies with `pip install -r requirements.txt` before running tests.
