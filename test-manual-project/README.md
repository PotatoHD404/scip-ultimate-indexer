# Manual Test Project

This root README exists to verify that markdown outside `docs/` still shows up in the index.

## What It Contains

- Python authentication code in `src/auth.py`
- Python database code in `src/database.py`
- Documentation in `docs/`
- Tests in `tests/`

## Search Targets

- Authentication flow
- Database pooling
- Installation instructions
- Contributing guidance

## Notes

The docs folder should not be drowned out by code-only results for documentation-heavy queries.
